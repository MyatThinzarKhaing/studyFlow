'use client'
import { useState } from 'react'
import { Check, Gauge } from 'lucide-react'
import { PageTitle } from '@/components/layout/AppShell'
export function QuizQuestion(){const [selected,setSelected]=useState('');const [submitted,setSubmitted]=useState(false);const options=['The interaction of genes and environment','Only inherited genetic traits','The effect of childhood experiences','A person’s social surroundings'];return <div className="flex flex-col gap-8"><PageTitle eyebrow="Practice quiz · 1 of 10" title="Check your understanding" action="Exit quiz"/><div className="mx-auto w-full max-w-2xl rounded-3xl bg-card p-7 shadow-sm"><div className="flex items-center justify-between text-sm text-muted-foreground"><span>Question 01</span><span className="flex items-center gap-1.5"><Gauge className="size-4"/>2 min</span></div><h2 className="mt-8 text-2xl font-semibold leading-snug">Which statement best describes the nature–nurture debate?</h2><div className="mt-7 flex flex-col gap-3">{options.map((option,i)=><button key={option} onClick={()=>!submitted&&setSelected(option)} className={`flex items-center gap-4 rounded-xl border p-4 text-left text-sm transition ${selected===option?'border-primary bg-primary/5':'border-border hover:bg-muted'} ${submitted&&i===0?'border-accent-foreground bg-accent/20':''}`}><span className="flex size-7 shrink-0 items-center justify-center rounded-full border border-border text-xs font-medium">{String.fromCharCode(65+i)}</span>{option}{submitted&&i===0&&<Check className="ml-auto size-4"/>}</button>)}</div>{submitted&&<p className="mt-5 rounded-xl bg-accent/20 p-4 text-sm text-accent-foreground">Correct. Most behavior reflects an ongoing interaction between biology and experience.</p>}<button disabled={!selected} onClick={()=>setSubmitted(true)} className="mt-7 w-full rounded-xl bg-primary py-3 text-sm font-medium text-primary-foreground disabled:opacity-40">{submitted?'Next question':'Submit answer'}</button></div></div>}
